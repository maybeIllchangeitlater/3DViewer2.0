#include "CPUTransformation.h"
namespace s21{

//void CPUTransformation::ApplyTranslation(QMatrix4x4 &matrix, float x, float y, float z)
//{
//    matrix(0,3) = x;
//    matrix(1,3) = y;
//    matrix(2,3) = -0.5 + z;
//}

//void CPUTransformation::ApplyScaling(QMatrix4x4 &matrix, float scale) {
//    matrix(0,0) *= scale;
//    matrix(1,1) *= scale;
//    matrix(2,2) *= scale;
//}

//void CPUTransformation::ApplyRotation(QMatrix4x4 &matrix, float x, float y, float z)
//{
//    x *= 3.1415926535 / 180;
//    y *= 3.1415926535 / 180;
//    z *= 3.1415926535 / 180;
//    matrix(0,0) = std::cos(x);
//    matrix(0,1) = -std::sin(x);
//    matrix(0,2) = std::sin(x);
//    matrix(1,0) = std::sin(y);
//    matrix(1,1) = std::cos(y);
//    matrix(1,2) = -std::sin(y);
//    matrix(2, 0) = -std::sin(z);
//    matrix(2, 2) = std::cos(z);
//    matrix(2,1) = std::sin(z);
//}

}
