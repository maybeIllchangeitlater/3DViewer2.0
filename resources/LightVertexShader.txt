#version 410 core


in vec3 position;

uniform mat4 projectionMatrix;
uniform mat4 modelViewMatrix;
uniform mat4 model;

void main()
{
    gl_Position = projectionMatrix * modelViewMatrix * model * vec4(position, 1.0);
}
